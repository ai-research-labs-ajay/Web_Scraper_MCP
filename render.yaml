services:
  - type: web
    name: ai-market-mcp
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn mcp_server:app --host 0.0.0.0 --port 10000"
    envVars:
      - key: API_KEY
        value: smartxalgo-secret
